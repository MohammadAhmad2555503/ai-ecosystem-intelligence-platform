# MLflow Container

This service runs a local MLflow tracking server.

Default URL:

```text
http://localhost:5000
```

The Step 6 A/B analysis code can log metrics to MLflow when this service is running.

